Can you create a test that does the following. It keeps replacing the network several times, while also rendering or initializing (if headless), and we observe the overall memory comsuption to see if there are memory leaks. Is there a good way to test this that make also sure that the garbage collector is doing the job correctly? if it increases by a certain amount then the test failed.