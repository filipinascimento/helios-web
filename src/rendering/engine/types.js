/**
 * @typedef {Object} FramebufferTarget
 * @property {'webgl2' | 'webgpu'} type
 * @property {number} width
 * @property {number} height
 * @property {WebGLFramebuffer | undefined} handle
 * @property {WebGLTexture | GPUTexture} texture
 */

// Dummy export to keep module consumers happy while only providing types.
export const __placeholder = null;
